import 'package:flutter/material.dart';
import 'package:image_circles/managers/firebase_manager.dart';

class CategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: FutureBuilder(
          // future: FirebaseManager.get,
          builder: (_, snap) {}),
    );
  }
}
